---
import astroLogo from '../assets/astro.svg';
import background from '../assets/background.svg';
---

<div class="relative flex flex-col items-center justify-center min-h-screen px-6 text-center bg-gray-50 overflow-hidden">
  <!-- Background gradient image with slow zoom animation -->
  <img 
    src={background.src} 
    alt="background gradient" 
    class="absolute inset-0 w-full h-full object-cover blur-3xl opacity-80 -z-10 animate-pulse-slow" 
    fetchpriority="high"
  />

  <!-- Logo with fade-in -->
  <a href="/" class="mb-8 animate-fade-in">
    <img src={astroLogo.src} width="130" height="52" alt="Logo" class="mx-auto" />
  </a>

  <!-- Hero Content -->
  <h1 class="text-4xl md:text-5xl font-extrabold leading-tight text-gray-900 max-w-3xl animate-slide-up delay-200">
    Hey, I’m 
    <span class="bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent animate-text-shimmer">
      Sarfaraj Rahman
    </span>
  </h1>

  <p class="mt-4 text-lg md:text-xl text-gray-600 max-w-xl animate-slide-up delay-400">
  I build web experiences that are as performant as they are beautiful. See for yourself: this landing page was crafted with Astro and Tailwind CSS.</p>

  <!-- CTA buttons with staggered animation -->
  <div class="mt-8 flex flex-wrap justify-center gap-4 animate-slide-up delay-600">
    <a 
      href="https://farazrahmn.vercel.app" 
      target="_blank"
      class="px-6 py-3 rounded-xl text-white font-medium shadow-lg bg-gradient-to-r from-blue-600 to-purple-600 transition-transform duration-300 hover:-translate-y-1 hover:shadow-xl animate-bounce-once"
    >
      🚀 View My Work
    </a>

    <a 
      href="https://farazrahmn.vercel.app" 
      target="_blank"
      class="flex items-center gap-2 px-6 py-3 rounded-xl text-gray-800 font-medium bg-white shadow-md transition-transform duration-300 hover:-translate-y-1 hover:shadow-lg animate-bounce-once"
    >
      Get in Touch
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" class="w-5 h-5">
        <path fill="currentColor" d="M12 2L3 21h18L12 2zm0 4.8 5.2 11.2H6.8L12 6.8z"/>
      </svg>
    </a>
  </div>

  <!-- Floating box with hover pop animation -->
  <a 
    href="/blog" 
    class="fixed bottom-6 right-6 max-w-xs p-4 bg-white/90 backdrop-blur-md rounded-xl shadow-xl hover:bg-white hover:scale-105 transition-transform duration-300 animate-fade-in"
  >
    <div class="flex items-center gap-3">
      <svg width="28" height="28" fill="none" xmlns="http://www.w3.org/2000/svg" class="text-blue-600">
        <path d="M5 12h14M12 5v14" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
      </svg>
      <div class="text-left">
        <h2 class="text-sm font-semibold text-gray-900">Latest Updates</h2>
        <p class="text-xs text-gray-600">Check out my recent experiments with Astro, Next.js, and Tailwind.</p>
      </div>
    </div>
  </a>
</div>

<style>
/* Tailwind animation extensions */
@keyframes fade-in {
  0% { opacity: 0; transform: translateY(10px); }
  100% { opacity: 1; transform: translateY(0); }
}

@keyframes slide-up {
  0% { opacity: 0; transform: translateY(20px); }
  100% { opacity: 1; transform: translateY(0); }
}

@keyframes text-shimmer {
  0% { background-position: -200% center; }
  100% { background-position: 200% center; }
}

@keyframes pulse-slow {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

@keyframes bounce-once {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-3px); }
}

.animate-fade-in { animation: fade-in 1s ease-out forwards; }
.animate-slide-up { animation: slide-up 1s ease-out forwards; }
.animate-text-shimmer { animation: text-shimmer 2s linear infinite; background-size: 200% auto; }
.animate-pulse-slow { animation: pulse-slow 10s ease-in-out infinite; }
.animate-bounce-once { animation: bounce-once 0.5s ease-in-out forwards; }
.delay-200 { animation-delay: 0.2s; }
.delay-400 { animation-delay: 0.4s; }
.delay-600 { animation-delay: 0.6s; }
</style>
